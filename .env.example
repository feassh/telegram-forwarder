# Telegram 配置
TELEGRAM_API_ID=your_api_id
TELEGRAM_API_HASH=your_api_hash
TELEGRAM_PHONE=+8613800138000

# 转发器类型: wecom（企业微信群机器人）, wecom-app（企业微信应用）, feishu（飞书）, custom（自定义API）
FORWARDER_TYPE=wecom

# 企业微信群机器人配置（当 FORWARDER_TYPE=wecom 时需要）
WECOM_WEBHOOK_URL=https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=your_key

# 企业微信应用配置（当 FORWARDER_TYPE=wecom-app 时需要）
WECOM_CORPID=wwxxx
WECOM_CORPSECRET=xxx
WECOM_AGENTID=100000
WECOM_TOUSER=@all  # 接收消息的用户，@all 表示所有人，或指定用户ID（账号）如: user1|user2

# 飞书配置（当 FORWARDER_TYPE=feishu 时需要）
FEISHU_WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/your_hook_id

# 自定义 API 配置（当 FORWARDER_TYPE=custom 时需要）
CUSTOM_API_URL=https://your-api.com/webhook
CUSTOM_API_METHOD=POST
CUSTOM_API_HEADERS=Authorization:Bearer token,X-Custom-Header:value

# 过滤配置
# 是否过滤免打扰的对话（true/false）
FILTER_MUTED=true

# 白名单（仅转发这些对话的消息，逗号分隔的 chat_id，留空表示不启用白名单）
WHITELIST_CHATS=

# 黑名单（不转发这些对话的消息，逗号分隔的 chat_id）
BLACKLIST_CHATS=